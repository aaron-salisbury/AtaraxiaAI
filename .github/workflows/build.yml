name: Build
on:
  push:
    branches: [modernize2026]
  pull_request:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - run: dotnet build AtaraxiaAI.sln --configuration Release
      - run: dotnet test AtaraxiaAI.sln --configuration Release --no-build
